{
  "name": "dropsolid/rocketship-drush10",
  "type": "project",
  "require": {
    "drush/drush": "^10.0",
    "twig/twig": "^2.12"
  },
  "scripts": {
    "post-install-cmd": [
      "cd vendor && find . -name \".git\" -type d -prune -exec rm -r \"{}\" \\; && cd .."
    ],
    "post-update-cmd": [
      "cd vendor && find . -name \".git\" -type d -prune -exec rm -r \"{}\" \\; && cd .."
    ]
  }
}
